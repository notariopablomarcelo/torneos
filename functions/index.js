// Cloud Functions Gen2. Por ahora vacío: el archivo existe para que el
// deploy de funciones no falle y para tener el lugar donde meter webhooks,
// endpoints públicos y jobs cuando hagan falta.

// Ejemplo (descomentar cuando se necesite):
// import { onRequest } from 'firebase-functions/v2/https';
// import { initializeApp } from 'firebase-admin/app';
// initializeApp();
// export const ping = onRequest({ invoker: 'public' }, (_req, res) => {
//     res.json({ ok: true });
// });
